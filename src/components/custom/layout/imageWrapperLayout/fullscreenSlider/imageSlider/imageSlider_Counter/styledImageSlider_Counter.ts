import styled from "styled-components";

export const StyledImageSlider_Counter = styled.p`
  position: absolute;
  top: 10px;
  left: 10px;

  padding-inline: 5px;
  font-weight: normal;
  background-color: ${(props) => props.theme.colors.common.backgroundColorAccent_Transparent};
`;
