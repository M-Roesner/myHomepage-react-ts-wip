export type ImageType = {
  src: string;
  srcFullSize: string;
  title?: string;
  alt?: string;
  // className?: string;
  // onClick?: () => void;
  // style?: React.CSSProperties;
  // loading?: "auto" | "eager" | "lazy";
  // onLoad?: () => void;
  // onError?: () => void;
  // ariaLabel?: string;
  // draggable?: boolean;
};
