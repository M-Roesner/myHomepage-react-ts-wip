export type ImageType = {
  id: number;
  src: string;
  srcFullSize: string;
  srcPreviewSize: string;
  title?: string;
  alt?: string;
  description?: string;
  // className?: string;
  // onClick?: () => void;
  // style?: React.CSSProperties;
  // loading?: "auto" | "eager" | "lazy";
  // onLoad?: () => void;
  // onError?: () => void;
  // ariaLabel?: string;
  // draggable?: boolean;
};
